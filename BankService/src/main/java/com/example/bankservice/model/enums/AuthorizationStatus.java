package com.example.bankservice.model.enums;

/**
 * Перечисление возможных состояний банковской авторизации.
 *
 * Отражает жизненный цикл операции — от успешной авторизации до отмены или сбоя.
 */
public enum AuthorizationStatus {
    /**
     * ✅ Средства успешно авторизованы (зарезервированы на карте).
     * Транзакция подтверждена банком.
     */
    AUTHORIZED,

    /**
     * ❌ Авторизация отклонена банком или платежной системой.
     * Деньги не зарезервированы.
     */
    DECLINED,

    /**
     * 🚫 Авторизация была аннулирована до списания средств.
     * Резерв средств снят, операция отменена.
     */
    VOIDED,

    /**
     * 💸 Средства были возвращены клиенту (refund).
     * Это обратная операция после успешной авторизации/списания.
     */
    REFUNDED,

    /**
     * ⚠️ Ошибка при выполнении авторизации (например, технический сбой или тайм-аут).
     * Операция не завершилась корректно.
     */
    FAILED
}
